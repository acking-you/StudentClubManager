使用到的技术：
1.登录界面利用计时器轮播图片
2.正则表达式验证输入正确性
3.MD5加密密码进行存储
4.MyBatis简化数据库的增删改查
5.使用HSSFWorkbook导入导出Excle

我的独特之处：
1.数据和界面分层，实现前后端分离（架构
2.所有的界面都是单例存在，实现类似于微服务的架构（架构
3.自定义异常处理，所以错误处理做的也很到位（错误处理
4.能用函数式简化的地方都用了函数式简化（代码风格
5.用到了多线程实现壁纸的轮播（拓展1
6.能导入导出Excel（拓展2
7.数据库只存储密码的MD5值（拓展3
8.实现各种顺序的排序（拓展4
8.自己重写了一些组件来使得功能更完善，界面更美观，比如超链接的label（拓展5

开发历程：
2022.2.23：
收到老师需要开发gui管理系统的指示。
之前没用Java做过Gui，只用C++做过，Java只做过Web开发。
然后花了大概一下午，熟悉了基本的Swing语法规范。
2022.2.24：
做了个登录界面，以及利用计时器实现了壁纸的轮播。
2022.2.25~2.27：
把基本功能全体实现，包括数据库的增删改查，以及数据输入用正则来验证。
然后简单的界面也成型了，密码采用MD5加密，数据支持Excle和txt文件的导入和导出。
最后再做了一些架构上的优化。
2022.2.28：
美化登录后的mainwindow界面。
并添加了很多标识自我的链接，这里需要重写一个JLable类来达到效果。
2022.6.3：
重构所有代码，使得前后端分离，将所有的界面进行单例处理，使得界面互不干扰，实现微服务的特点。

(┬┬﹏┬┬) 不得不说，前端的优化过程真的对Swing来说太过艰难